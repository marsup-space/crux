name: crux
description: A terminal-based AI coding agent with agentic orchestration.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  nocterm:
    path: nocterm
  path: ^1.9.0
  toml: ^0.18.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
